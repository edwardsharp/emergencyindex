<%= search_form_for @q, url: '/admin/projects-list', remote: true, class: 'row' do |f| %>
  <div class="input-field-fix col s3 l2">
	  <%= f.check_box :published_true %>
	  <%= f.label :published_true, 'Published' %>
	</div>
	<div class="col s3 l4">
  	<%= f.select :volume_year_in, Volume.all.collect {|p| [ p.year, p.year ] }.unshift(['Volume','Volume']), { disabled: 'Volume'}, multiple: true %>
	</div>
	<div class="input-field col s4 l5">
	  <%= f.search_field :name_or_title_or_venue_or_home_or_first_date_or_tags_name_or_description_cont %>
	  <%= f.label :name_or_title_or_venue_or_home_or_first_date_or_tags_name_or_description_cont, "Search" %>
	</div>
	<div class="input-field col s2 l1">
		<button class="btn waves-effect waves-light tooltipped" type="submit" name="submit" data-tooltip="Search Projects">
	    <i class="material-icons">search</i>
	  </button>
	</div>
<% end %>

 <!--  <div class="col s2">
    <button id="new-user-modal-trigger" class="waves-effect waves-light btn tooltipped" data-tooltip="Create New Project"><i class="material-icons">add</i></button>
  </div>
 -->


<div id="projects-list">
	<%= render 'projects_list' %>
</div>

