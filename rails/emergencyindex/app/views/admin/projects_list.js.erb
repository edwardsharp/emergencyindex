$('#projects-list').html("<%= j render('projects_list', projects: @projects) %>");
$('.tooltipped').tooltip();
<% if @projects.blank? %>
$('#project-pub').html($('#q_published_true').prop('checked') ? 'published' : 'unpublished');
<% end %>

